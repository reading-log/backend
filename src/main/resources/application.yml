spring:
  profiles:
    active: dev

  jpa:
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true

logging.level:
  org.hibernate.SQL: debug
  root: info
  com.api.readinglog: DEBUG